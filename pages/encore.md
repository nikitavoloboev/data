## cmd
```
encore run - run
encore gen client -e local -o ../web/encore-client.ts - generates client to web
# don't forget with above that localhost will be breaking so have to also swap out localhost with real ip address (mac thing)
```